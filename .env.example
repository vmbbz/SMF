# Required — Deepgram API key for STT (Flux v2) and TTS (Aura 2)
DEEPGRAM_API_KEY=

# Required for LLM fighter mode — at least one
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Redis — required for multiplayer rooms, matchmaking queue, signaling
REDIS_URL=redis://localhost:6379

# Postgres — required for ELO ratings, leaderboard, match history
DATABASE_URL=postgresql://stick:fighter@localhost:5433/stickfighter

# OAuth login (optional — leaderboard tracking requires login)
# dev       https://id.local.deepgram.com:4202
# staging   https://id.staging.dx.deepgram.com
# production  https://id.dx.deepgram.com
OIDC_ISSUER=https://id.local.deepgram.com:4202
OIDC_CLIENT_ID=stick-fighter
OIDC_CLIENT_SECRET=
OIDC_REDIRECT_URI=http://fight.local.deepgram.com:3007/auth/callback

# Required for Phone mode — Twilio call-in
TWILIO_PHONE_NUMBERS=+15551234567,+15551234568
BASE_URL=https://your-ngrok-url.ngrok.io

# Market Data APIs
BIRDEYE_API_KEY=
# Optional server-side candidate activity evidence. Bounded HTTP polling is the
# free default. The key is appended only to private backend RPC requests and
# never exposed to web/mobile code.
ALCHEMY_STREAM_ENABLED=0
ALCHEMY_API_KEY=
ALCHEMY_STREAM_TRANSPORT=solana_http_polling
ALCHEMY_SOLANA_WS_ENDPOINT=wss://solana-mainnet.g.alchemy.com
ALCHEMY_SOLANA_HTTP_ENDPOINT=https://solana-mainnet.g.alchemy.com
# PubSub is optional and must pass the production method-support proof gate.
# Yellowstone is paid/credits-only. Its endpoint is ignored in HTTP/PubSub mode.
ALCHEMY_YELLOWSTONE_ENDPOINT=https://solana-mainnet.g.alchemy.com
ALCHEMY_STREAM_FRESHNESS_SECONDS=20
ALCHEMY_STREAM_ACTIVITY_WINDOW_SECONDS=180
ALCHEMY_STREAM_MAX_CANDIDATES=32
ALCHEMY_STREAM_CANDIDATE_REFRESH_SECONDS=180
ALCHEMY_STREAM_REWIND_SLOTS=32
# Free-tier bounded HTTP signature history; not a live subscription or native
# replay. One getSlot plus one request per candidate runs every 180 seconds.
ALCHEMY_STREAM_POLL_INTERVAL_SECONDS=180
ALCHEMY_STREAM_BACKFILL_MAX_SLOTS=512
ALCHEMY_STREAM_BACKFILL_LIMIT_PER_CANDIDATE=1000
ALCHEMY_STREAM_BACKFILL_MAX_PAGES_PER_CANDIDATE=2
ALCHEMY_STREAM_BACKFILL_EXTRA_PAGE_BUDGET=8
ALCHEMY_STREAM_BACKFILL_MIN_INTERVAL_SECONDS=60
# Used only by the optional Yellowstone transport.
ALCHEMY_STREAM_MAX_REPLAY_SLOTS=6000
# Optional endpoint hardening:
# Prefix for market endpoints consumed by app clients.
SMF_MARKET_API_PREFIX=/api/marketfeed/v2
# Keep legacy endpoints (/api/trending, /api/graduates, /api/token/{mint}) disabled by default.
SMF_ALLOW_LEGACY_MARKET_ENDPOINTS=0

# Solana boost economy
# Purchases remain disabled until the launched game-token mint, reward vault,
# transfer builder, and transfer verifier are all configured and verified.
BOOST_PAYMENT_TOKEN_MINT=
BOOST_PAYMENT_TOKEN_SYMBOL=TOKEN
BOOST_PURCHASES_ENABLED=0
BOOST_SETTLEMENT_MODE=reward_vault_transfer
BOOST_PAYMENT_TOKEN_PRICE_FALLBACK=0
# Legacy deployment fallback only. Prefer BOOST_PAYMENT_TOKEN_MINT.
SMF_MINT=
# Public token identities and public treasury accounts. Never put private keys here.
ANSEM_TOKEN_MINT=
GAME_TOKEN_REWARD_VAULT=
ANSEM_REWARD_VAULT=
CREATOR_FEE_PAYOUT_WALLET=
OPERATING_TREASURY_WALLET=
# Optional fixed quote price for controlled local tests only.
# BOOST_PAYMENT_TOKEN_FIXED_PRICE_USD=
# Production defaults. For a tiny mainnet smoke test only, set BOOST_MICRO_USD_CENTS=1 locally.
BOOST_MICRO_USD_CENTS=100
BOOST_DEGEN_USD_CENTS=300
BOOST_CHAOS_USD_CENTS=500
# Recommended: private provider endpoint (e.g. Alchemy) instead of shared public RPC.
SOLANA_RPC=https://solana-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY
# Public/client-safe RPC URL returned to browsers/mobile WebView.
SOLANA_RPC_PUBLIC=https://api.mainnet-beta.solana.com
STARTER_BOOSTS=15
BOOST_INTENT_TTL_SECONDS=600
WALLET_AUTH_CHALLENGE_TTL_SECONDS=300
WALLET_AUTH_SESSION_TTL_SECONDS=86400

# Android Digital Asset Links identity. Override only if package name or signing key changes.
ANDROID_APP_PACKAGE_NAME=com.solanamemefighter.app
ANDROID_APP_SHA256_CERT_FINGERPRINT=84:86:97:57:2F:90:2C:DC:01:7B:30:C3:87:D3:D2:A8:8D:47:E4:11:CA:B9:54:BA:B1:05:95:98:9D:DE:1D:76
